# ============================================================
# Poliwise Environment Configuration
# Copy this file to .env and replace all placeholder values.
# NEVER commit real secrets to version control.
# ============================================================

# PostgreSQL
# Use a strong, randomly generated password in production
POSTGRES_PASSWORD=<generate-random-32-char-password>

# RabbitMQ
RABBITMQ_PASSWORD=<generate-random-32-char-password>

# Redis
REDIS_PASSWORD=<generate-random-32-char-password>

# MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=<generate-random-32-char-password>
MINIO_BUCKET_NAME=poliwise-documents

# JWT
# Must be at least 256 bits (32+ chars) for HS256
JWT_SECRET=<generate-random-64-char-hex-string>

# Service-to-service authentication (do not reuse either value as a user role)
INTERNAL_SERVICE_TOKEN=<generate-random-64-char-hex-string>
INTERNAL_API_KEY=<generate-random-64-char-hex-string>

# OnlyOffice document editing
ONLYOFFICE_JWT_SECRET=<generate-random-64-char-hex-string>
ONLYOFFICE_REDIS_PASSWORD=<generate-random-32-char-password>
ONLYOFFICE_DOCUMENT_SERVER_URL=http://localhost:8888
ONLYOFFICE_CALLBACK_URL=http://knowledge-service:8083/api/v1/documents
ONLYOFFICE_CALLBACK_PUBLIC_URL=http://api-gateway:3001/api/v1/documents
ONLYOFFICE_LOCK_DURATION_MINUTES=30
ONLYOFFICE_URL=http://localhost:8888

# AI / Embedding (LitServe backend)
# Self-hosted BGE-M3 model endpoint (see services/ai/README.md)
LITSERVE_EMBEDDING_URL=http://localhost:8001
LITSERVE_RERANKER_URL=http://localhost:8002

# LLM API (Groq for Metadata Suggestion Phase 1)
GROQ_API_KEY=<your-groq-api-key>
GROQ_MODEL=llama-3.3-70b-versatile  # Recommended for fast JSON structured outputs

# Optional bootstrapping. Leave disabled after the initial controlled deployment.
ADMIN_BOOTSTRAP_ENABLED=false
ADMIN_USERNAME=
ADMIN_EMAIL=
ADMIN_PASSWORD=

# Email is opt-in. Keep credentials empty while disabled.
EMAIL_ENABLED=false
EMAIL_USERNAME=
EMAIL_APP_PASSWORD=
EMAIL_FROM_NAME=Poliwise Support
EMAIL_FROM_ADDRESS=support@poliwise.example

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:3001

# Ingestion Service
INGESTION_SERVICE_PORT=8088
LITSERVE_EMBEDDING_URL=http://localhost:8001
LITSERVE_RERANKER_URL=http://localhost:8002
